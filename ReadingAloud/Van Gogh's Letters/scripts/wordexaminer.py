def WordToExamine(word): 
	def __init__():
		this.word = word

	
	is(): #returns the word initialized

		return this.word 

	def _first(arg = None): #you might as well write this to accept both no arguments
		
		if arg == None:
	
			return this.word[0]

		else:
			numb = arg-1

			return this.word[numb:]
 

	def _last(arg = None):
		
		if arg == None:

			length = len(this.word)-1
		
			return this.word[length]

		else:
			
			length = arg - 1
			

			return this.word[:length]






			
		