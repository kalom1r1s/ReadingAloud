def Txt(opened_txtfile,section_markoff = None): #using either File.open(src, "a") or open (src, "a")

    def __init__(self, sections, paragraphs, sentences):
        if section_cue == None:

            this.sections = None

        else:

            this.sections = opened_txtfile.read().split(section_markoff)

        this.paragraphs = opened_txtfile.read().split('\n')

        this.sentences = opened_txtfile.read().split(". ")

        #clean paragraphs up later, meaning remove the entries of the list
        #that generate empty values based on your paragraph split() criteria

    amountof_sections():

        return len(this.sections)

    amountof_paragraphs():

        return len(this.paragraphs)

    get_paragraph_no(number):

        if 
